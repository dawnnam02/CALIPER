# 다음에 할 일

2026-09-01 기준. 위에서부터 순서대로.

---

## 지금 상태

**깃허브에 올라가 있다** — https://github.com/dawnnam02/CALIPER (비공개)
테스트 53개 · **독립 캠페인 6종** (설계 551,172개 · 표적 단백질 15종)

구조가 둘로 갈렸다 — `caliper/` 는 검증된 기여, `caliper/harness/` 는 개발 기록.

이번까지 해소된 것: 표적 확대, 단계 상관 측정, README 재작성, 교정 주장 정정,
계층 교정 검증, 다양성 측정, 깃허브 푸시, **다중 라운드 측정 후 기각**.

---

## 확정된 결과 (더 손댈 필요 없음)

| | |
|---|---|
| **교차점** | 고정 풀에서는 진다(d=−0.73), 예산 일치에서는 이긴다(d=+1.17, 9/10 표적). 배수 3.0× |
| **원인** | 단계 상관 0.55~0.66 + 값싼 단계와의 AUC 격차(상관 −0.652). 이론 예측과 일치(Tang 1988) |
| **계층 교정** | 실데이터 검증 완료. 세 전략 중 최악인 적이 없음. 전환점 웰 20개 |
| **교정 정직성** | 인샘플 0.000은 과적합이었고 교차검증 0.293이 진짜. 소표본에선 확률 보고를 거부 |
| **다양성** | 이 데이터로는 검증 불가 (여러 캠페인 혼합이라 원래 다양함) |
| **다중 라운드** | **기각.** 천장 +0.015, 웰 24개로는 도달 불가. 교정은 순위를 못 바꾼다 |
| **탐색 할당량** | **기각.** 평균 이득 없음. 대신 **기울기 검사**(무료)가 파국을 잡는다 |
| **기울기 감지기** | ✅ **독립 캠페인 6종 검증.** 단위 22개 기준 민감도 0.833 [0.552, 0.953] · 정밀도 1.000 (보조 지표까지 넣으면 0.839) |

---

## ❌ 측정하고 기각한 것 — 다시 시도하지 말 것

### 탐색 할당량 (가장 독창적이라던 주장)
**실데이터에서 이득 없음.** 19셀 중 18셀 무승부, 쌍대 검정 유의차 없음
(diff +0.041, CI [−0.017, +0.142]).

한 셀(EGFR@24)에서만 파국을 막았는데, 원인을 파보니 **웰을 안 쓰고 공짜로 감지**할
수 있는 것이었다 — 라벨 표본이 좁은 띠만 덮으면 Platt 기울기가 뒤집히고(−17.1),
그 곡선은 미실험 설계를 전부 96%로 예측한다(실제 4.6%).

`check_calibration()` 이 19셀에서 **파국 1개만 정확히 거부**했다. 오탐 0.
**탐색 할당량은 버리고 기울기 검사를 남긴다.**

### 다중 라운드 지표 전환
**만들기 전에 쟀고, 재보니 만들면 안 되는 것이었다.**

먼저 개념적 문제: **교정은 순위를 바꾸지 못한다.** 단조 변환이라 "몇 개를 보낼까"만
바꾸고 "누구를 보낼까"는 그대로다. 라운드 1 라벨이 바꿀 수 있는 건 **어떤 점수로
줄을 세우느냐** 하나뿐이다.

그건 실제로 여지가 있다 — 표적 10개에서 최고 지표가 6종류로 갈리고 전역 최고는
4/10에서만 최선이다. 그런데 실측 결과:

| 2라운드 정책 | 적중률 |
|---|---|
| oracle (진짜 최고를 앎) | 0.239 |
| static (안 바꿈) | 0.225 |
| switch (순진한 argmax) | 0.220 |
| guarded (내 안전장치) | 0.215 |

- **천장이 +0.015밖에 안 된다**
- 웰 24개는 양성 중앙값 9개 → 순진한 규칙의 정답률 27.8%
- **guarded가 한 27번의 전환 중 진짜 최고를 고른 건 0번**, 그리고 안 바꾸는 것보다 나빴다
  (관측 격차가 큰 것만 고르면 **가장 심하게 과적합된 추정**을 고르게 된다)

`caliper/multiround.py` 는 이 부정 결과의 기록으로만 남겨뒀다. 어디에도 연결 안 됨.
**실무 조언: 다른 표적들에서 최고였던 지표로 줄 세우고, 웰 24개짜리 한 라운드가
그걸 뒤집게 두지 마라.**

---

## 🔴 남은 것 — 우선순위 순

### 1. `run.py` 를 실데이터 경로와 통합
지금 `run.py` 는 시뮬레이터 전용이고, 실데이터는 `experiments/` 에만 있다.
`config.yaml` 에 `backend: overath` 를 추가해 하나로 합치는 게 맞다.

---

## 🟡 여유 있을 때

### 4. 다양성 제어
**단일 캠페인 데이터가 생기기 전까지는 보류.** 붙일 거면 ProtDBench 방식
(Foldseek 구조 클러스터링, 서열 동일성 컷오프 없음).

### 5. 비용 상수
AF2-IG 16.4s 를 1.0 으로 정규화한 상대비용을 쓰고 있다. ColabFold 5.2× 는
Mirdita 2022 에서 유도한 값이라 하드웨어가 다르다. AF3 는 공개 수치가 없다.
**적중률은 이 값들과 무관하므로 급하지 않다.** 배수만 흔들린다.

### 6. 공개 전환
```powershell
gh repo edit --visibility public --accept-visibility-change-consequences
```
README 를 한 번 읽어보고 판단할 것.

---

## 확인된 공백 (시간 쓰지 말 것)

| | |
|---|---|
| ProteinMPNN 1단계 소급 검증 | 공개 데이터에 pMPNN 점수+실험결과가 같이 없다. Overath 설계에 직접 돌리는 수밖에 |
| 배분 정책 벤치마크 | 존재하지 않음. 베이스라인은 내가 만든 것 |
| GPU-시간/검증 바인더 | 어느 파이프라인도 보고 안 함 |
| eta=3 wet-lab 검증 | 없음 |
| 캐스케이드 탈상관 임계값 | 이론은 연속적. 하드 컷오프는 존재하지 않음 (Tang 1988) |

---

## 파일 지도

```
CALIPER/
├── README.md          교차점이 헤드라인
├── DESIGN.md          모든 상수의 출처
├── CRITIQUE.md        68건 + 1·2·3차 수정 로그
├── NEXT.md            이 파일
├── push.ps1 / push.sh 깃허브 푸시
├── caliper/
│   ├── whentocascade.py  단계를 붙일지 말지 규칙 (실데이터 유래)
│   ├── multiround.py     지표 전환 — 측정 결과 쓰지 말 것. 연결 안 됨
│   ├── hierarchical.py   표적별 교정 ★검증됨
│   ├── smallsample.py    Venn-Abers/Beta/Platt + 거부 게이트
│   ├── baselines.py      경쟁 스케줄러 5종
│   ├── stats.py          쌍대 검정 · 교차검증 · 구간
│   └── benchmarks.py     문헌 수치 (실행시간·AUC·임계값·표적패널)
├── experiments/
│   ├── real_data.py           ★ 주 결과 (leave-one-target-out)
│   ├── budget_matched.py      ★ 교차점
│   ├── why_cascade_lost.py    ★ 원인 규명 + 규칙
│   ├── hierarchical_value.py  ★ 계층 교정 검증
│   ├── multiround.py          ★ 다중 라운드 (부정 결과)
│   ├── diversity_check.py     다양성 측정
│   └── validate.py            시뮬레이터 40시드
└── data/overath/SOURCE.md     CSV 재다운로드 링크 (82MB, gitignore)
```
