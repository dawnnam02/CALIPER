# 다음에 할 일

2026-09-01 중단 시점 기준. 위에서부터 순서대로 하면 된다.

---

## 지금 상태 한 줄

**실데이터(Overath n=3,650, 15표적)로 평가가 끝났고, 결과가 갈렸다.**
연산이 공짜면 캐스케이드가 **지고**, 예산이 묶이면 **이긴다.**
테스트 37개 통과. 커밋 완료. 깃허브 푸시만 남음.

---

## 🔴 1순위 — 결론을 확정하는 데 필요

### 1-1. 예산 일치 실험의 표적을 늘린다
지금 **표적 4개**뿐이라 결정적 결과(d=1.33)의 신뢰구간이 너무 넓다.
`experiments/budget_matched.py` 의 `n<120` 조건 때문에 11개가 걸러진다.

- **할 것**: 표적별 부트스트랩 재표본으로 작은 표적도 포함시키거나,
  전체를 풀링해 계층 모형으로 추정
- **판단 기준**: 표적 10개 이상에서 유의하면 주장 확정, 아니면 철회

### 1-2. 단계 상관을 측정한다
실데이터에서 캐스케이드가 진 이유가 **단계 간 상관** 때문이라는 게 내 가설인데
**아직 안 쟀다.**

- **할 것**: `af2_pae_interaction` / `colab_ipSAE_min` / `af3_ipSAE_min` 의
  스피어만 상관행렬을 실데이터로 계산
- **왜**: 시뮬레이터는 단계 잡음을 **독립**으로 뒀다. 그게 캐스케이드에 가장
  유리한 가정이고, 실데이터가 그걸 부정했다. 숫자로 확인해야 한다
- **그다음**: 측정한 상관을 시뮬레이터에 넣어 실데이터 결과가 재현되는지 본다.
  재현되면 시뮬레이터가 고쳐진 것이고, 안 되면 다른 원인이 있다

### 1-3. README를 실데이터 결과로 다시 쓴다
**지금 README는 시뮬레이터 숫자만 있다.** 실데이터가 그중 일부를 뒤집었는데
반영이 안 돼 있다. **이 상태로 푸시하면 안 된다.**

- 시뮬레이터 표 → 실데이터 표로 교체 (시뮬레이터는 보조로)
- "고정임계값보다 낫다" → **"고정된 풀에서는 진다. 예산이 묶이면 이긴다"**
- 교정 주장 강도 낮추기 (아래 2-1과 함께)

---

## 🟠 2순위 — 정직성 문제

### 2-1. 교정 주장을 재작성한다
`caliper/smallsample.py` 로 등장회귀는 이미 교체했고 게이트도 걸었다.
그런데 **README·DESIGN·CRITIQUE에는 옛 주장이 남아 있다.**

- 실데이터 결과: 표적별 예측-실제 괴리가 크다
  (TrkA 예측 0.307 / 실제 0.071, Mdm2 예측 0.339 / 실제 0.573)
- **할 것**: "교정한다"가 아니라 **"표적을 넘어서는 교정은 전이되지 않는다"**
  를 결과로 보고. 이게 실측으로 확인된 진짜 발견이다

### 2-2. `HierarchicalCalibrator` 를 실데이터로 검증한다
표적이 15개 생겼으니 **이제 검증이 가능하다.** 지금까지는 표적이 1개라 못 했다.

- leave-one-target-out 으로 전역 교정 vs 계층 교정 비교
- **판단 기준**: 계층이 전역보다 나으면 유지, 아니면 삭제

### 2-3. `CRITIQUE.md` 에 2차 발견을 추가한다
2차 비판에서 찾은 4건이 문서에 없다.
- `explore_fraction=0` 인데 160개 탐색한 버그
- 지표 선택이 신호를 죽인 문제
- 40시드 전까지 주장이 유의하지 않았던 것
- 기저율 +11% 편향

---

## 🟡 3순위 — 마무리

### 3-1. 깃허브 푸시
**막혀 있다.** `gh` 미설치, 원격 없음, 자격증명 없음.

```bash
winget install GitHub.cli
gh auth login
gh repo create CALIPER --public --source=. --push
```
푸시 전에 `pyproject.toml` 의 `Source` URL 의 `USERNAME` 을 고칠 것.

### 3-2. 비용 상수를 실제 값으로
`unit_cost` (1.0 / 8.0 / 20.0) 는 **추정치다.**
ProteinDJ 논문(Silke 2025, *Proteins*)에 실제 초 단위가 있다 —
`pMPNN 1.3s · AF2IG 16.4s · Boltz-2 8.6s · RFD 54.9s · BindCraft 620.5s`

이게 오면 절감률이 처음으로 의미를 갖는다. 지금은 비율이 틀리면 결론도 틀린다.

### 3-3. 다양성 제어
24개 웰이 거의 같은 서열로 채워질 수 있다. 지금 **아무 장치도 없다.**
ProtDBench 의 클러스터 기준을 받아오면 그대로 적용한다.

---

## 아직 못 하는 것 (확인된 공백)

| | 왜 |
|---|---|
| ProteinMPNN 1단계 필터 평가 | **어떤 공개 데이터에도 pMPNN 점수 + 실험결과가 같이 없다.** Overath 설계에 직접 돌리는 수밖에 없음 |
| 실제 도구 연동 | GPU·가중치 필요. `external.py` 는 일부러 예외를 던진다 |
| GPU-시간 대비 성능 | 어느 논문도 보고하지 않음 |
| eta=3 검증 | wet-lab 검증이 존재하지 않음 |

---

## 받아오면 좋은 논문 (우선순위)

1. **ProteinDJ** (Silke 2025, *Proteins*) — 단계별 실제 실행시간 → 3-2
2. **ProtDBench** (arXiv:2605.04118) — 검증자 간 일치도 + 클러스터 기준 → 1-2, 3-3
3. **Manokhin 2026** 소표본 교정 벤치마크 — Venn-Abers 순위 → 2-1
4. **Riley 2021** (Stat Med 40(19):4230) — `pmsampsize` 공식 → 게이트 임계값
5. **BenchBB** (Adaptyv) — 표준 표적 패널

---

## 파일 지도

```
CALIPER/
├── README.md          ← 1-3에서 다시 써야 함
├── DESIGN.md          ← 설계 근거 + 출처
├── CRITIQUE.md        ← 68건 + 수정로그. 2-3에서 추가 필요
├── NEXT.md            ← 이 파일
├── config.yaml        ← 사용자가 만지는 유일한 파일
├── run.py             ← 단일 캠페인 (시뮬레이터)
├── caliper/
│   ├── smallsample.py ← 소표본 교정 + 거부 게이트 (신규)
│   ├── calibrate.py   ← 등장회귀 (n>=1000일 때만)
│   ├── hierarchical.py← 표적별 교정. 2-2에서 검증 필요
│   ├── baselines.py   ← 경쟁 스케줄러 4종
│   ├── stats.py       ← 쌍대 검정 · 교차검증 · 신뢰구간
│   └── benchmarks.py  ← 문헌 수치 모음
├── experiments/
│   ├── validate.py       ← 시뮬레이터, 40시드
│   ├── real_data.py      ← 실데이터, leave-one-target-out ★
│   └── budget_matched.py ← 예산 일치 비교 ★ 1-1 대상
└── data/overath/
    └── SOURCE.md      ← CSV는 gitignore. 링크로 재다운로드
```
