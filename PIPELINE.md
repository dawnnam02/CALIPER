# 단백질 결합체 설계 — 단계별 상세

각 단계가 **무엇을 하는지 · 왜 그런지 · 어디서 틀리는지**.
설정값은 전부 [`pipeline/step0_config.py`](pipeline/step0_config.py) 한 곳에 있다.

---

## 전체 흐름

```
표적 구조 (PDB)
    │
 1. 표적 준비        핫스팟 고르기, 잘라내기          ← 사람이 판단
    │
 2. 골격 생성        RFdiffusion                      GPU · 수 시간
    │                골격 1,000개
 3. 서열 설계        ProteinMPNN                      수 분
    │                서열 8,000개
 4. 구조 검증        AlphaFold2 initial guess         GPU · 가장 오래
    │                점수 8,000줄
 5. 필터와 순위      pae · plddt · rmsd               ← 사람이 판단
    │                100~800개 (표적에 따라 0개)
 6. 실험 발주        다양성 정리 + 대조군 + 배치      수 초
    │
 96웰 플레이트  →  실험실
    │
    └──→ 결과가 나오면 validation/check_filters.py 로 임계값 재조정 → 2라운드
```

**계산 비용의 90% 이상이 4단계에 있다.** 1~3단계는 상대적으로 싸다.
그래서 "골격을 더 만들까"는 대체로 값싼 선택이고,
"AF2 를 더 돌릴까"는 비싼 선택이다.

---

## 1. 표적 준비

> `python pipeline/step1_target.py`

### 하는 일
표적 구조를 읽고, 지정한 핫스팟이 말이 되는지 확인하고, 주변만 잘라낸다.

### 핫스팟이 뭔가
결합체가 **반드시 닿았으면 하는 표적 잔기**들. 3~5개를 고른다.

지정하지 않으면 RFdiffusion 이 표적 아무 면에나 붙는 골격을 만든다.
대부분은 우리가 원하지 않는 면이다.

### 좋은 자리 / 나쁜 자리

| | 조건 | 왜 |
|---|---|---|
| ✓ | 오목한 곳 (주머니, 홈) | 평평한 면은 붙잡을 데가 없다 |
| ✓ | 소수성 잔기 (Leu Ile Val Phe Trp Met Tyr) | 물을 밀어내며 붙는 힘이 결합의 주력 |
| ✓ | 기능적으로 의미 있는 자리 | 붙어도 아무 일 안 일어나면 소용없다 |
| ✓ | 표면에 노출된 곳 | 파묻힌 잔기는 접근 불가 |
| ✗ | 평평하고 극성인 면 | 붙을 이유가 없다 |
| ✗ | 당사슬(glycan) 자리 | 구조 파일엔 안 보이는데 실제론 당이 달려 있다 |
| ✗ | 유연한 고리·말단 | 흔들려서 예측이 안 맞는다 |
| ✗ | 결정화 인공물 | 결정 안에서만 생기는 접촉면 |

스크립트가 소수성 여부와 파묻힘 정도는 계산해 준다.
**기능적 의미는 사람이 논문을 읽고 판단해야 한다. 자동화 못 한다.**

### 자주 틀리는 것
- **AlphaFold 예측 구조를 표적으로 쓴다** → 예측 오차 위에 설계를 쌓게 된다.
  실험 구조(RCSB PDB)가 있으면 반드시 그걸 써라.
- **핫스팟을 너무 많이 지정한다** → 다 만족시키는 골격이 없어 생성이 실패한다.
- **당사슬 자리를 고른다** → 계산에서는 완벽한데 실험에서는 접근조차 못 한다.
  UniProt 의 PTM 항목을 확인해라.

---

## 2. 골격 생성 (RFdiffusion)

> `python pipeline/step2_backbone.py`

### 골격이 뭔가
단백질은 두 층이다.

- **골격** = 사슬이 공간에서 어떤 모양으로 접혔는가 (뼈대)
- **서열** = 그 자리마다 어떤 아미노산이 오는가 (살)

이 단계는 뼈대만 만든다. 열쇠로 치면 홈 모양을 먼저 깎는 것이다.
나온 PDB 의 아미노산이 전부 글라이신(GLY)인 건 정상이다.

### RFdiffusion 이 하는 일
그림 생성 AI 와 같은 확산 모델(diffusion model)이다. 노이즈에서 시작해
조금씩 걷어내며 단백질 모양을 만든다. 결합체에서는 조건을 하나 건다 —
*"표적의 이 핫스팟들에 닿는 모양이어야 한다."*

### 자주 틀리는 것

**노이즈 스케일을 0으로 안 둔다.**
기본값은 1.0이다. 다양성을 위한 것인데 결합체에서는 그 다양성이
성공률을 떨어뜨린다는 게 측정돼 있다 (Watson et al. 2023 의 binder 절).
`RFDIFFUSION_NOISE_SCALE = 0.0` 이 그것이다. **가장 흔하게 놓치는 설정.**

**골격을 너무 적게 만든다.**
뒤에서 90% 이상 걸러진다. 100개로 시작하면 실험할 게 한 자리 수로 남는다.

**생성이 자꾸 실패한다.**
핫스팟이 서로 멀거나 파묻혀 있을 가능성이 높다. 1단계로 돌아가라.

---

## 3. 서열 설계 (ProteinMPNN)

> `python pipeline/step3_sequence.py`

### 하는 일
**역접힘(inverse folding)** — 구조를 주고 서열을 받는다.
AlphaFold 가 하는 일(서열→구조)의 반대다.

### 골격 하나에 서열 8개를 뽑는 이유
같은 모양을 만드는 서열은 여러 개다. 어떤 건 잘 발현되고 어떤 건 안 되고,
어떤 건 잘 녹고 어떤 건 뭉친다. **만들기 전엔 모른다.**
그래서 여러 개 뽑아 두고 다음 단계에서 거른다.

### soluble 가중치를 꼭 써야 하는 이유
기본 ProteinMPNN 은 PDB 구조로 학습됐다. 그런데 PDB 구조 대부분은
**다른 단백질과 붙어 있거나 막에 박혀 있는 것**이라 표면에 소수성 잔기가 많다.

그 편향을 그대로 배우면 혼자 물에 떠 있어야 할 우리 결합체 표면에도
기름칠을 한다. 결과는 **"발현은 됐는데 전부 침전"** 이다.
soluble 가중치는 그 편향을 줄여 학습한 것이다.

### 이 단계에서 미리 거르는 것
- **시스테인 홀수 개** — C 는 둘씩 짝지어 다리를 만드는데, 홀수면 하나가 남아
  다른 분자와 엉겨붙어 뭉친다
- **같은 글자 5개 이상 연속** — DNA 합성이 어렵고 발현도 나쁘다
- **길이 초과**

---

## 4. 구조 검증 (AlphaFold2 initial guess)

> `python pipeline/step4_predict.py`

### 이 단계가 파이프라인의 성패를 가른다

지금까지는 "이런 모양이면 붙겠다"고 **가정**하고 만들었다.
여기서 **다른 모델**로 그 가정을 다시 확인한다.

설계에 쓴 것(RFdiffusion, ProteinMPNN)과 검증에 쓰는 것(AlphaFold2)이
서로 달라야 한다. 같은 모델로 만들고 채점하면 자기 실수를 못 잡는다.
문제 낸 사람이 자기 답안을 채점하는 셈이다.

### `initial guess` 가 뭔가 — 이 한 줄이 성공률을 10배 올렸다

보통 AF2 는 **서열만 보고** 구조를 처음부터 예측한다. 결합체처럼
자연에 없는 단백질에는 잘 안 통한다. 참고할 진화 정보(MSA)가 없기 때문이다.

initial guess 는 다르게 묻는다.

> "우리가 설계한 이 복합체 구조에서 출발해라.
>  이 서열이 정말 이 모양이 맞다면, 너도 여기 머물 것이다."

설계가 맞으면 AF2 가 근처에 머물고, 틀리면 멀리 도망간다.
그 '머물렀는가'를 재는 것이다.

> 근거: Bennett et al. 2023, *Nature Communications* 14:2625.
> 이 한 가지로 실험 성공률이 약 10배 올랐다.

### 나오는 값

| 값 | 방향 | 실측 농축 | 비고 |
|---|---|---|---|
| `pae_interaction` | 낮을수록 | **2.2~2.4×** | 주 필터 |
| `binder_aligned_rmsd` | 낮을수록 | 1.1~2.6× | 데이터셋에 따라 갈림 |
| `plddt_binder` | 높을수록 | **0.9~1.4×** | 거의 일 안 함 |
| `iptm` | 높을수록 | — | 참고 |

→ [VALIDATION.md](VALIDATION.md)

### 시간
가장 오래 걸린다. 서열 하나에 GPU 로 수십 초.
서열 8,000개면 **며칠**이다. 중간에 끊겨도 끝난 건 다시 안 한다.

---

## 5. 필터와 순위

> `python pipeline/step5_filter.py`

### 교과서 임계값
```
pae_interaction < 10  ·  plddt_binder > 80  ·  rmsd < 2.0 Å
```

### 이 파이프라인이 다른 점 — 조용히 적용하지 않는다

실측 데이터 60만 개로 확인한 결과, **같은 임계값인데 표적마다
통과율이 0%에서 98%까지 벌어졌다.**

스크립트는 순서를 이렇게 잡는다.

1. 필터를 하나씩 누적해 걸며 **몇 개가 죽는지 먼저 보고**
2. 20개 미만이 남으면 **멈추고**, 데이터에서 계산한 대안 임계값을 제안
3. 통과한 것을 `pae_interaction` 순으로 정렬

### 필터가 전부 죽일 때

실측에서 실제로 일어났다 — Bennett/Tie2 는 92,293개 중 **0개** 통과.

이럴 때 선택지는 둘이다.

**(가) 임계값을 푼다**
실험할 게 생기지만 적중률이 떨어진다.
`step5_filter.py` 가 "상위 100개가 되려면 임계값이 얼마여야 하는지"를
데이터에서 계산해 알려준다.

**(나) 1단계로 돌아간다**
통과가 0에 가까우면 이쪽을 **먼저** 의심해라.
필터가 전부 죽인다는 건 보통 "설계가 부족하다"가 아니라
**"그 자리에 붙는 모양이 애초에 없다"**는 뜻이다.
핫스팟을 다시 고르거나, 표적의 다른 면을 봐라.

골격을 10배 더 만드는 건 대개 답이 아니다. GPU 시간만 열 배 쓴다.

### 순위를 오해하지 말 것
`pae` 가 낮다는 건 **AF2 가 확신한다**는 뜻이지 **실제로 붙는다**는 뜻이 아니다.
실측 농축이 2배 남짓이라는 게 그 의미다 — 도움은 되지만 보장은 아니다.

---

## 6. 실험 발주

> `python pipeline/step6_order.py`

### 상위 96개를 그냥 보내면 안 되는 이유

골격 하나에서 서열 8개를 뽑았기 때문에, 순위 상위 96개는 대개
**골격 열댓 개에서 나온 형제 서열들**이다.
그 골격 하나가 틀린 가정이면 8개가 통째로 같이 실패한다.
96웰을 썼는데 실제로는 12가지만 시험한 셈이 된다.

그래서 서열 유사도 90%를 넘는 것끼리는 하나만 남긴다.

### 대조군을 반드시 넣는 이유

설계 96개가 전부 안 붙었다고 하자. 원인이 뭔가?

1. 설계가 나빴다
2. 표적 단백질이 죽어 있었다
3. 실험 조건이 잘못됐다

**대조군이 없으면 이 셋을 구분할 방법이 없다.**

- **양성 대조군** (이미 붙는 걸 아는 것 — 기존 항체, 천연 리간드)
  → 이게 안 붙으면 **실험이 잘못된 것**이다. 설계 탓하지 마라
- **음성 대조군** (안 붙는 게 확실한 무관한 단백질)
  → 이게 붙으면 비특이 결합이다. 전체 결과를 의심해야 한다

웰 4개가 아깝게 느껴지지만, 이게 없으면 실패했을 때 **아무것도 배우지 못한다.**
그 판이 통째로 낭비가 된다.

대조군은 플레이트의 **서로 떨어진 모서리**에 놓는다. 한 구석에 몰면
그 구석에 생긴 문제(가장자리 증발 등)와 대조군 실패를 구분할 수 없다.

### 보내기 전 확인
- [ ] 대조군 서열을 실제로 채워 넣었는가 (스크립트는 자리만 잡아둔다)
- [ ] 발현 태그를 붙일 것인가 (His-tag, Avi-tag)
- [ ] 발주처의 서열 제한을 확인했는가
- [ ] 이 표적에 알려진 결합체가 있으면 양성 대조군으로 넣었는가

---

## 라운드가 끝나면

실험 결과가 나오면 **그게 이 표적에 대한 첫 진짜 데이터**다.

`plate.csv` 에 `binding` 열을 추가하고 (붙었으면 1, 아니면 0):

```bash
python validation/check_filters.py --csv plate.csv --name 내표적
```

네 표적의 농축 배수가 공개 캠페인 넷과 **나란히** 찍힌다.
네 숫자만 보고 임계값을 정하지 말고 다른 표적들의 폭도 같이 봐라 —
같은 임계값이 표적에 따라 0%에서 98%까지 벌어진다는 게 이 저장소의 결론이다.

거기서 나온 값이 2라운드의 임계값이 된다.
문헌값은 **1라운드까지만 쓰는 값**이다.

---

## 필요한 도구

| 도구 | 단계 | 필요한 것 | 받는 곳 |
|---|---|---|---|
| RFdiffusion | 2 | GPU (VRAM 8GB+), 가중치 ~2GB | [RosettaCommons/RFdiffusion](https://github.com/RosettaCommons/RFdiffusion) |
| ProteinMPNN | 3 | CPU 가능(느림), 가중치 ~100MB | [dauparas/ProteinMPNN](https://github.com/dauparas/ProteinMPNN) |
| AF2 initial guess | 4 | GPU 필수, 파라미터 ~4GB | [nrbennet/dl_binder_design](https://github.com/nrbennet/dl_binder_design) |

설치 후 위치를 알려준다:

```bash
export RFDIFFUSION_DIR=/받은/경로
export PROTEINMPNN_DIR=/받은/경로
export AF2_INITIAL_GUESS_DIR=/받은/경로
```

지금 무엇이 깔려 있는지 확인:

```bash
python pipeline/run_all.py --check
```

**도구가 없으면 각 단계는 무엇을 설치해야 하는지 알려주고 멈춘다.
가짜 결과를 만들어내지 않는다.**
