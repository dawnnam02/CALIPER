# 검증 — 정석 필터가 실제로 얼마나 걸러주는가

> 다시 재보려면: `python validation/check_filters.py`
> 이 문서의 모든 숫자는 그 스크립트가 실측 데이터에서 계산한 것이다.

---

## 왜 이 문서가 있는가

교과서와 논문은 이렇게 말한다.

```
pae_interaction < 10  ·  plddt_binder > 80  ·  rmsd < 2.0 Å
```

이 값들은 **실제 실험 결과가 붙어 있는 캠페인**에서 나왔다. 문제는
**그 값이 내 표적에서도 맞는지는 아무도 확인해 주지 않는다**는 것이다.

그래서 실험 결과가 공개된 캠페인 네 개에 이 필터를 그대로 걸어봤다.

| 캠페인 | 설계 | 결합 | 기저 적중률 | 있는 열 |
|---|---|---|---|---|
| Bennett et al. 2023 | 603,178 | 23,044 | 3.82% | pae · plddt · rmsd |
| Overath et al. 2025 | 3,669 | 394 | 10.74% | pae · plddt · rmsd |
| Adaptyv EGFR R2 | 380 | 55 | 14.47% | pae · plddt |
| BindCraft 2025 | 152 | 65 | 42.76% | pae · plddt |

**농축 배수 = (필터 통과한 것의 적중률) ÷ (전체 적중률)**
1.0배면 필터가 아무 일도 안 한 것이고, 1.0 미만이면 **해로운** 것이다.

---

## 결과 1 — 필터 세 개가 하는 일이 서로 다르다

| 캠페인 | 필터 | 통과 | 통과율 | 적중률 | 농축 |
|---|---|---|---|---|---|
| Bennett | `pae < 10` | 38,553 | 6.4% | 8.3% | **2.2×** |
| Bennett | `plddt > 80` | 493,558 | 81.8% | 3.6% | **0.9×** ⚠ |
| Bennett | `rmsd < 2.0` | 23,436 | 3.9% | 9.9% | **2.6×** |
| Overath | `pae < 10` | 981 | 26.7% | 26.1% | **2.4×** |
| Overath | `plddt > 80` | 2,304 | 62.8% | 14.7% | 1.4× |
| Overath | `rmsd < 2.0` | 3,047 | 83.0% | 11.8% | 1.1× |
| Adaptyv | `pae < 10` | 105 | 27.6% | 22.9% | 1.6× |
| Adaptyv | `plddt > 80` | 222 | 58.4% | 17.6% | 1.2× |

### 읽는 법

**`pae_interaction` 이 유일하게 두 데이터셋 모두에서 일관되게 일한다.**
2.2× 와 2.4×. 이게 이 파이프라인이 pae 를 주 필터로 쓰는 이유다.

**`plddt > 80` 은 가장 널리 쓰이는데 가장 일을 안 한다.**
Bennett 60만 개에서 **0.9×** — 통과한 쪽이 오히려 *덜* 붙었다.
81.8%를 통과시키니 거른다고 하기도 어렵다.
버리진 않되, **이걸로 거르고 있다고 착각하면 안 된다.**

**`rmsd` 는 데이터셋에 따라 갈린다.** Bennett 2.6×(가장 셈) 대 Overath 1.1×.
두 데이터셋이 rmsd 를 다르게 정의해서일 가능성이 크다
(Bennett `AF2_complex_RMSD` 대 Overath `af2_binder_aligned_rmsd`).
같은 이름의 지표라도 정의를 확인하고 써야 한다.

### 덤으로 확인한 것 (Bennett 60만 개)

| 필터 | 농축 | 판정 |
|---|---|---|
| `AF2_complex_RMSD < 1.0` | **4.2×** | 아주 셈. 다만 통과가 0.1%뿐 |
| `Rosetta ddG < -50` | 1.1× | **거의 무의미** |
| `DAN_interface_lddt > 0.8` | **0.6×** | **해로움** |

Rosetta 계면 에너지가 무의미하게 나온 건 Bennett 논문 자체의 주장과 일치한다.
AF2 가 Rosetta 를 대체한 이유가 이것이다.

---

## 결과 2 — 임계값 10은 대체로 맞다. 더 조이면 나빠진다

`pae` 임계값을 바꿔가며:

| 캠페인 | <5 | <7.5 | **<10** | <15 | <20 |
|---|---|---|---|---|---|
| Bennett | 2.4× | 2.3× | **2.2×** | 1.9× | 1.8× |
| Overath | 2.2× | 2.3× | **2.4×** | 2.2× | 1.9× |
| Adaptyv | — | **0.0×** | **1.6×** | 1.2× | 1.1× |

**교과서 값 10이 나쁘지 않다.** 조여도 크게 좋아지지 않는다.

**그런데 Adaptyv 를 봐라.** `<7.5` 로 조이면 통과가 **5개**로 줄고
그 5개는 **하나도 안 붙었다**(0.0×). 조일수록 좋아진다는 직관이
작은 데이터에서는 정면으로 깨진다. 남는 개수가 두 자리 밑으로 떨어지면
농축이든 뭐든 그냥 우연이다.

---

## 결과 3 — 세 개를 합치면?

| 캠페인 | 통과 | 통과율 | 적중률 | 농축 |
|---|---|---|---|---|
| Bennett | 14,341 | 2.4% | 11.3% | **3.0×** |
| Overath | 944 | 25.7% | 26.2% | **2.4×** |

Bennett 에서는 합친 게 낫다 (2.6× → 3.0×).
**Overath 에서는 `pae` 하나와 완전히 같다** (2.4× → 2.4×).
필터 세 개를 걸었는데 두 개가 아무 일도 안 한 것이다.

---

## 결과 4 — 여기가 진짜 중요하다: 표적마다 전혀 다르다

같은 `pae < 10` 을 표적별로 걸었을 때.

| 캠페인/표적 | 전체 | 통과 | 통과율 | 적중률 | 농축 | |
|---|---|---|---|---|---|---|
| Bennett/Tie2 | 92,293 | **0** | 0.0% | — | — | **전멸** |
| Overath/VirB8 | 99 | **0** | 0.0% | — | — | **전멸** |
| Bennett/H3 | 57,381 | 2 | 0.0% | 0.0% | **0.0×** | **해로움** |
| Bennett/SARS_CoV2_RBD | 98,991 | 69 | 0.1% | 4.3% | **14.4×** | 훌륭 |
| Overath/SARS_CoV2_RBD | 99 | 2 | 2.0% | 100% | **11.0×** | 훌륭 |
| Overath/EGFR | 434 | 10 | 2.3% | 30.0% | 4.7× | |
| Overath/FGFR2 | 2,123 | 247 | 11.6% | 42.1% | 4.6× | |
| Bennett/IL7Ra | 14,888 | 1,325 | 8.9% | 8.5% | 2.7× | |
| Bennett/FGFR2 | 59,343 | 4,768 | 8.0% | 43.3% | 1.6× | |
| Bennett/EGFR | 98,746 | 5,168 | 5.2% | 0.5% | 1.4× | |
| Overath/IL7Ra | 171 | 107 | 62.6% | 32.7% | 1.5× | |
| Bennett/TrkA | 14,982 | 1,572 | 10.5% | 14.8% | 1.1× | 거의 무의미 |
| Overath/Mdm2 | 96 | 86 | 89.6% | 58.1% | **1.0×** | 무의미 |
| Overath/Pdl1 | 95 | 93 | 97.9% | 12.9% | **1.0×** | 무의미 |

### 읽는 법

**통과율이 0%에서 98%까지 벌어진다.** 같은 숫자 하나를 걸었는데.

- **Tie2**: 92,293개 설계 중 **0개** 통과. 라이브러리가 통째로 죽는다.
  골격을 10배 더 만들어도 소용없다.
- **Pdl1**: 97.9% 통과. 필터를 안 건 것과 같다.
- **H3**: 2개 통과, 둘 다 안 붙음. 필터가 오히려 방해했다.
- **SARS**: 99.9%를 죽이지만 남은 것은 14배 좋다. 이런 표적에서는 필터가 최고다.

**그래서 파이프라인은 임계값을 조용히 적용하지 않는다.**
`pipeline/step5_filter.py` 가 통과 개수를 먼저 보고하고,
20개 미만이면 **멈춰서** 데이터에서 계산한 대안 임계값을 제안한다.

---

## 이 검증의 한계 — 반드시 같이 읽을 것

**1. BindCraft 는 이 검증에 쓸 수 없다.**
`pae < 10` 을 걸었더니 152개가 **전부** 통과했다(1.0×). 임계값을 5로 조여도
전부 통과했다. 이유는 명백하다 — **BindCraft 가 공개한 것은 자기네 필터를
이미 통과한 설계들**이다. 걸러진 것에 다시 필터를 거니 할 일이 없다.
표에 남겨둔 건 이런 데이터가 왜 검증에 안 맞는지 보여주기 위해서다.

**2. 다른 캠페인들도 정도의 차이일 뿐 선별된 집합이다.**
아무도 설계 전부를 실험하지 않는다. 실험된 것은 이미 어떤 기준으로 뽑힌
것들이다. 그러니 여기 나온 농축 배수는 **"필터가 없는 진짜 라이브러리"에서의
값이 아니라, "이미 한 번 걸러진 것 위에서 다시 걸었을 때"의 값**이다.
실제 파이프라인에서는 더 셀 수도 있다.

**3. 결합의 정의가 캠페인마다 다르다.**
Bennett 은 효모 표면제시 avidity, Overath 는 원 논문들의 판정,
Adaptyv 는 자체 BLI 측정이다. 같은 "붙었다"가 아니다.

**4. 표적 21개는 적은 표본이다.**
"통과율 0%가 두 번 나왔다"는 것은 확실하지만, **"내 표적에서 몇 %일까"**는
이 표로 알 수 없다.

---

## 결론 세 줄

1. **`pae_interaction < 10` 은 쓸 만하다.** 두 큰 데이터셋에서 2.2×, 2.4×.
2. **`plddt > 80` 은 거의 일을 안 한다.** Bennett 에서 0.9×. 쓰되 믿지는 마라.
3. **어떤 임계값도 표적을 가로질러 옮겨지지 않는다.** 그래서 이 파이프라인은
   필터를 걸기 전에 몇 개가 죽는지 먼저 보고하고, 전멸이면 멈춘다.

**네 표적에서 이걸 다시 확인하는 방법은 하나뿐이다** — 1라운드를 실제로 돌리고,
그 결과를 `validation/check_filters.py` 에 넣는 것. 그때 나온 임계값이
2라운드의 임계값이 된다.
